import React from 'react'
import students from './data'

const Student = () => {
    
    return (
        <div>
            students.map(i){
i.name
            }
        </div>
    )
}

export default Student
